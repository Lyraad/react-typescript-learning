import React from "react";

function AddMessage() {
  // const {addPost} = props;
  return (
    <>
      <form action="submit">
        <input type="text" />
        <button>Submit</button>
      </form>
    </>
  );
}
export default AddMessage;
